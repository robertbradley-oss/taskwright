import {hash} from './scenario.js';
import {summarizeEligibility,validateEligibilityCalibration} from './eligibility-experiment.js';
export function eligibilityArchiveFiles(report,seal,calibration){if(seal.reportHash!==hash(report)||seal.planHash!==report.plan.hash||seal.calibrationHash!==calibration.hash||report.plan.controls.calibrationHash!==calibration.hash||!validateEligibilityCalibration(calibration,report.plan.controls.cliVersion).passed)throw Error('Eligibility seal or calibration differs');if(report.rows.some(r=>['queued','running'].includes(r.run?.status))||hash(summarizeEligibility(report.plan,report.rows))!==hash(report))throw Error('Eligibility result does not reproduce');const {plan}=report,files=[],add=(path,value)=>files.push({path,value});for(const c of Object.values(plan.contracts))add(`eligibility-contracts/${c.id}.json`,c);add(`eligibility-experiments/${plan.id}.json`,plan);if(plan.configuration.id==='support-candidate'&&plan.configuration.version>1)add(`configurations/support-candidate-${plan.configuration.version}.json`,plan.configuration);for(const row of report.rows){if(row.run)add(`${row.run.id}.json`,row.run);if(row.review)add(`reviews/${row.runId}-${row.review.id}.json`,row.review);}for(const f of files)if(!/^(?:configurations\/support-candidate-\d+|(?:eligibility-contracts|eligibility-experiments)\/[0-9a-f-]{36}|reviews\/[0-9a-f-]{36}-[0-9a-f-]{36}|[0-9a-f-]{36})\.json$/.test(f.path))throw Error('Invalid archive path');if(new Set(files.map(f=>f.path)).size!==files.length)throw Error('Duplicate record');return files;}
