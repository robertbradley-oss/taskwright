const el=(tag,text)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;return n;};
export function renderDiagnostics(root,records){for(const d of records||[]){const box=el('details');box.append(el('summary',`Failure diagnostics · ${d.stage} · ${d.code}`),el('p',d.message),el('p',`Call ${d.call??'not started'} · exit ${d.exitCode??'unavailable'}`));if(d.details)box.append(el('pre',JSON.stringify(d.details,null,2)));for(const name of ['response','stdout','stderr']){const c=d[name];if(!c)continue;box.append(el('h4',name==='response'?'Rejected agent message':name),el('p',`${c.retainedBytes}/${c.bytes} observed bytes retained${c.truncated?' · truncated':''}. Hash covers observed bytes only.`),el('pre',c.text||'(empty)'));}root.append(box);}}
